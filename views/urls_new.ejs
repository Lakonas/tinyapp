<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <link rel="stylesheet" href="/styles/style.css" />
    <title>ShortStop - Create New URL</title>
  </head>

  <body>
    <%- include('partials/_header') %>

    <% if (user) { %>
      <main>
        <h3>Create Short URL</h3>
        <form action="/urls" method="POST">
          <div class="form-group">
            <label for="longURL">Enter a URL:</label>
            <input
              class="form-control"
              type="text"
              name="longURL"
              id="longURL"
              placeholder="https://example.com"
              required
            />
          </div>
          <button type="submit" class="btn btn-primary">Create ShortStop URL</button>
        </form>
      </main>
    <% } else { %>
      <main>
        <div class="alert alert-warning">
          <p>You need to be logged in to create a new URL.</p>
          <p>Please <a href="/login">log in</a> or <a href="/register">register</a>.</p>
        </div>
      </main>
    <% } %>
  </body>
</html>